{
  "name": "cm-strength-dynasty-korean-html",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate": "python tools/generate_project.py",
    "preview": "npx http-server . -p 4173",
    "export": "node export-pdf.js",
    "verify": "python tools/verify_project.py",
    "audit": "python tools/audit_missing_content.py",
    "qa": "python tools/qa_production.py",
    "visual-audit": "python tools/visual_fidelity_audit.py",
    "final-check": "npm run generate && npm run export && npm run verify && npm run audit && npm run qa && npm run visual-audit"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "playwright": "^1.53.0"
  }
}
